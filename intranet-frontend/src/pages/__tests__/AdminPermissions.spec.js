import { describe, it, expect } from 'vitest'
import AdminPermissions from '../AdminPermissions.vue'

describe('AdminPermissions component', () => {
  it('loads the component', () => {
    expect(AdminPermissions).toBeTruthy()
  })
})
