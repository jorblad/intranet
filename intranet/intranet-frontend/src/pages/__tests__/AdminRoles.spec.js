import { describe, it, expect } from 'vitest'
import AdminRoles from '../AdminRoles.vue'

describe('AdminRoles component', () => {
  it('loads the component', () => {
    expect(AdminRoles).toBeTruthy()
  })
})
